info:
  name: patch
  type: http
  seq: 2

http:
  method: PATCH
  url: http://127.0.0.1:8000/key_value/1234/hello?is_public=false
  params:
    - name: value
      value: new_value
      type: query
      disabled: true
    - name: is_public
      value: "false"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
