info:
  name: Add User to Item
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseUrl}}/key_value/:project_id/:key/access/:user_id"
  params:
    - name: key
      value: test_key
      type: path
    - name: user_id
      value: dffad742-fbf6-4a61-9346-1a159f1dac6c
      type: path
    - name: project_id
      value: "1234"
      type: path
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://127.0.0.1:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
